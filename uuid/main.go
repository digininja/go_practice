package main

import "fmt"
import "github.com/google/uuid"

func main() {
	fmt.Printf("UUID: %s\n", uuid.New())
	fmt.Printf("UUID: %s\n", uuid.New())
	fmt.Printf("UUID: %s\n", uuid.New())
	fmt.Printf("UUID: %s\n", uuid.New())
}
